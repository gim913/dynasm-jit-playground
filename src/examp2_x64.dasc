// DynASM directives.
|.arch x64
|.section code
|.actionlist actions

void dynasmGenerator(Dst_DECL)
{
		// dasm understandable code is here
		| mov rax, num
		| inc rax
        | shl rax, 1
		| ret
}
